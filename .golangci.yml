version: "1.54"
run:
  timeout: 5m
  tests: true
linters:
  enable:
    - govet
    - gosimple
    - staticcheck
    - ineffassign
    - typecheck
    - gocritic
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
